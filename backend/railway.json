{
  "build": {
    "env": {
      "NIXPACKS_BUILD_CMD": "apt-get update && apt-get install -y nodejs npm && npm install --prefix frontend && npm run build --prefix frontend"
    }
  },
  "start": "ng serve --host 0.0.0.0 --port 4200 --prefix frontend"
}

